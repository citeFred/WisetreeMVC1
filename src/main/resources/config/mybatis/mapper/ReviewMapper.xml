<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.review.mapper.ReviewMapper">
	<insert id="addReview" parameterType="Review">
		<selectKey keyProperty="num" resultType="int" order="BEFORE">
			select review_seq.nextval from dual
		</selectKey>
		insert into review(renum, userid, content, score, filename, wdate, pidx_fk)
		values(#{renum}, #{userid}, #{content:VARCHAR}, #{score}, #{filename:VARCHAR}, sysdate, #{pidx_fk})
	</insert>
	
	<select id="listReview" resultType="Review" parameterType="int">
		select * from review where pidx_fk=#{value} order by renum desc
	</select>
</mapper>