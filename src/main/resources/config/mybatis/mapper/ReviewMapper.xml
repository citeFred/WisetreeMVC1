<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.review.mapper.ReviewMapper">
	<select id="listReview" resultType="Review" parameterType="int">
		select * from review where pidx_fk=#{value} order by renum desc
	</select>
	
	<select id="getReviewCnt" resultType="_int" parameterType="_int">
		select count(renum) from review where pidx_fk=#{value}
	</select>
	
	<insert id="addReview" parameterType="Review">
		<selectKey keyProperty="renum" resultType="int" order="BEFORE">
			select review_seq.nextval from dual
		</selectKey>
		insert into review(renum, userid, content, score, refilename, wdate, pidx_fk)
		values(#{renum}, #{userid}, #{content:VARCHAR}, #{score}, #{refilename:VARCHAR}, sysdate, #{pidx_fk})
	</insert>
	
	<select id="getReview" resultType="Review" parameterType="_int">
		select * from review where renum=#{value}
	</select>
	
	<delete  id="delReview" parameterType="_int">
		delete from review where renum=#{value}	
	</delete>
	
	<update id="upReview" parameterType="Review">
		update review set userid=#{userid}, content=#{content:VARCHAR}, score=#{score:NUMERIC}
			<if test="filename!=null and filename!=''">, filename=#{filename} </if>
		where renum=#{renum}
	</update>
	
	
</mapper>